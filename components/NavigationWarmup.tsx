"use client";

/**
 * Route-level loading.tsx files provide the loading experience.
 * Keep this component inert so it cannot interfere with WKWebView link taps.
 */
export function NavigationWarmup() {
  return null;
}
